# Infrastructure scripts

Cross-platform lifecycle scripts live in `tools/scripts` because they coordinate
both workspace tooling and local infrastructure. Provider-specific deployment
automation will be added here only after a deployment target is approved.
