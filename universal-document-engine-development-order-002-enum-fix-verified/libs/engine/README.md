# Engine boundaries

The future document engine is independent of both user interfaces. This order
creates only named architectural boundaries; it deliberately defines no document
model, orchestration contract, parser behavior, rule syntax, provider integration,
validation policy, or output-generation behavior.

Contracts will be introduced when a real use case supplies enough evidence to
avoid speculative abstractions.
